{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://velinux.local/schemas/pkg-scheme.json",
  "title": "vlx pkg scheme",
  "description": "Package scheme definition for vlx pkg",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "repos": {
      "type": "array",
      "description": "Zypper repositories to add before installing packages",
      "items": {
        "type": "object",
        "required": ["alias", "url"],
        "additionalProperties": false,
        "properties": {
          "alias": {
            "type": "string",
            "description": "Repository alias"
          },
          "url": {
            "type": "string",
            "format": "uri",
            "description": "Repository URL (.repo file)"
          }
        }
      }
    },
    "zypper": {
      "type": "array",
      "description": "Packages to install via zypper",
      "items": {
        "type": "string"
      }
    },
    "flatpak": {
      "type": "array",
      "description": "Packages to install via flatpak",
      "items": {
        "type": "string"
      }
    },
    "pre": {
      "type": "array",
      "description": "Shell commands to run before installing packages",
      "items": { "type": "string" }
    },
    "post": {
      "type": "array",
      "description": "Shell commands to run after installing packages",
      "items": { "type": "string" }
    }
  }
}
