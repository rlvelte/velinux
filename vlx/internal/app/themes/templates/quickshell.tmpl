pragma Singleton
import QtQuick
import Quickshell

Singleton {
    readonly property color primary: "{{.Primary}}"
    readonly property color primaryDim: "{{.PrimaryDim}}"
    readonly property color primarySubtle: "{{.PrimarySubtle}}"
    readonly property color primaryMuted: "{{.PrimaryMuted}}"

    readonly property color primarySelected: Qt.rgba(primary.r, primary.g, primary.b, 0.14)
    readonly property color primaryHovered:  Qt.rgba(primary.r, primary.g, primary.b, 0.07)

    readonly property color secondary: "{{.Secondary}}"
    readonly property color secondaryDim: "{{.SecondaryDim}}"
    readonly property color secondaryLight: "{{.SecondaryLight}}"

    readonly property color accent: "{{.Accent}}"
    readonly property color accentDim: "{{.AccentDim}}"
    readonly property color accentLight: "{{.AccentLight}}"

    readonly property color base: "{{.Base}}"
    readonly property color mantle: "{{.Mantle}}"
    readonly property color crust: "{{.Crust}}"
    readonly property color surface0: "{{.Surface0}}"
    readonly property color surface1: "{{.Surface1}}"
    readonly property color surface2: "{{.Surface2}}"

    readonly property color text: "{{.Text}}"
    readonly property color subtext: "{{.Subtext}}"
    readonly property color overlay: "{{.Overlay}}"
    readonly property color muted: "{{.Muted}}"

    readonly property color success: "{{.Success}}"
    readonly property color warning: "{{.Warning}}"
    readonly property color warningSubtle: "{{.WarningSubtle}}"
    readonly property color error: "{{.Error}}"
    readonly property color errorSubtle: "{{.ErrorSubtle}}"
    readonly property color info: "{{.Info}}"
    readonly property color infoSubtle: "{{.InfoSubtle}}"

    readonly property color textOnPrimary: "{{.OnPrimary}}"
    readonly property color textOnSecondary: "{{.OnSecondary}}"
    readonly property color textOnAccent: "{{.OnAccent}}"
    readonly property color textOnSurface: "{{.OnSurface}}"

    readonly property string fontName: "{{.FontName}}"
    readonly property string fontNameHeading: "{{.FontNameHeading}}"
    readonly property string fontNameMono: "{{.FontNameMono}}"
    readonly property int fontSize: {{.FontSize}}
    readonly property int fontSizeSmall: {{.FontSizeSmall}}
    readonly property int fontSizeLarge: {{.FontSizeLarge}}
    readonly property int fontSizeHeading: {{.FontSizeHeading}}

    readonly property string logoPath: "{{.LogoPath}}"
}
